import type { Express, Request, Response } from "express";

export function registerImageRoutes(_app: Express): void {
  // Endpoint removed — DALL-E image generation is not used by any client screen.
}
